* {
    margin: 0;
    padding: 0;
}

#navigator_line {
    font-size: 10px;
    padding-top: 3px;
    margin-bottom: 15px;
}
#title {
    float: left;
}

#upper_right {
    float: right;
    font-weight: bold;
    margin-bottom: 5px;
    margin-top: 5px;
    font-size:medium;
    color: #555;
}

body a{
    text-decoration:none;
    color: #1d3688;
}

body a:hover{
    text-decoration:underline;
}

body{
    min-width:960px; /* was 896 and then 911*/
    background-color:#fff;
    font-family: verdana, sans-serif;
    font-size: small;
    margin-left: 2%;
    margin-right: 2%;
}

h2 {
    margin-top: 10px;
    margin-bottom: 5px;
}

h3 {
    /*margin-top: 10px;*/
    margin-bottom: 5px;
    font-size: medium;
    font-weight: bold;
}

h4 {
    /*margin-top: 10px;*/
    margin-bottom: 5px;
    font-size: small;
    font-weight: bold;
}

#head {
    min-width: 960px; /* 896*/
    background-image: url(images/bg1.png);
    border-right: 1px solid #ccc;
    border-left: 1px solid #ccc;
}

#head_middle {
    background: #fbfbfb;
    min-width: 940px;
    padding-right: 9px;
    padding-left: 9px;
}


#main {
    margin-top: 20px;
    margin-bottom: 20px;
    margin-left: 10px;
    margin-right: 10px;
}

#main p {
    padding-bottom: 10px;
}

#main td {
    padding-right: 10px;
}

#main th {
    text-align: left;
    padding-right: 10px;
    vertical-align: top;
}

#main > ul {
    padding-left: 25px;    
}

/*BEGIN NEW NAVIGATION*/

    #menu{                         /* menu list container */
        list-style-type: none;      /* disable the display of the list item bullets */
        margin: 0;                /* space around the list container */
        padding: 0;               /* space within the list container */
        position: static;           /* need this so that the z-index stuff works correctly */       
        color: #555;                /* the default font color (not links) within the main menu container */
        z-index: 20;                /* push the menu up in the layer order a bit so it isn't hidden behind anything */
    }

    #menu > li{                      /* top-level menu element */
        list-style-type: none;      /* disable the display of the list item bullets */
        float: left;                /* this is to allow for the horizontal main menu */
        /*background-color: #eee; */    /* main menu item background color */
        color: #555;                /* main menu item font color (not links) */
        margin: 0;                /* spacing between main menu items */
        padding: 0;               /* padding within main menu items */
        width: 119px;               /* the width of each main menu item */
        display: block;
        border-right: 1px solid #ccc;
        /*height:20px;*/
        height: 1.6em;
    }

    #menu li ul li {
   /*     border-bottom: 1px solid #ccc;*/
    }

    #menu ul {                     /* third-level (or greater) menu element list elements */
        position: relative;         /* this is so that it doesn't push that page content around on hover */
        margin: 0;                /* space around the list container */
        padding: 0;               /* space within the list container */
        list-style-type: none;      /* disable the display of the list item bullets */
        display: none;
        width: 119px;
        top: 0;
        left: -1px;
        background-color: #eee;     /* sub-menu default background color */
        background-image: none;
        z-index: 99;                /* want to be sure this is above the rest of the menu */
        border-left: 1px solid #ccc;
        border-right: 1px solid #ccc;
        border-bottom: 1px solid #ccc;
    }

    #menu ul li{                   /* second-level or greater menu element links */
        background-color: #eee;     /* default background color for sub-menu container */
        color: #555;                /* default font color (not links) for sub-menu container */
        border: none;               /* sub-menu item border settings */
        margin: 0;                /* spacing between sub-menu containers */
    }

    #menu li a{                    /* top-level menu element links */
        text-align: left;         /* text alignment in main menu item links */
        /*width: 112px;*/
        width: 99px;
        display: block;
        color: #555;
        font-size: 12px;
        padding: 3px 10px 2px 10px;
        margin: 0;
    }


    #menu > li > a {                   /* all the other level menu link elements */
        height: 1.3em; /* was 16*/
    }

    #menu a:hover,                 /* top-level hovering properties */
    #menu li:hover{
        display: block;
        background-color: #ddd;
    }

    /*not in use*/
    #menu ul ul{                   /* higher-level list containers */
        display: none;              /* don't display by default */
        position: absolute;
        /*margin-left: 122px;   */      /* this should be the width of #dmenu ul li */
        margin-top: -2em;           /* this will push the sub-menu up to the level of it's parent */
    }

    /* only non-MSIE browsers use this */
    #menu ul li>ul,
    #menu ul ul li>ul{
        margin-top: -2em;           /* should be set to the same as #dmenu ul ul margin-top */
    }


    /* additional sub-menu levels in the next 2 blocks. (For up to 5 levels of drop menus) */
    #menu li:hover ul ul,
    #menu li:hover ul ul ul,
    #menu li:hover ul ul ul ul,
    #menu li:hover ul ul ul ul ul{
        display:none;
    }

    #menu li:hover ul,
    #menu ul li:hover ul,
    #menu ul ul li:hover ul,
    #menu ul ul ul li:hover ul,
    #menu ul ul ul ul li:hover ul{
        display:block;
    }

    li>ul {
        top: auto;
        left: auto;
    }



/*END NEW NAVIGATION*/



#navigator_line2 {
    min-width: 940px;
    position: relative;
    font-size: 12px;

    padding-top: 3px;
    padding-bottom: 2px;
    padding-left: 10px;
    padding-right: 10px;
    /*background: #eee;*/
    background-image: url(images/bg1.png);
/* height: 15px;*/
    height: 1.3em;
}

#navigator_line2_menu{
    position: absolute;
    left: 0;
    padding-left: 10px;
}

#navigator_line2_update{
    position: absolute;
    right: 0;
    padding-right: 10px;
}

.on {
    font-weight: bold;
    text-decoration: underline;
}

#navigator_line2 a{
    color: #1d3688;
}

#buttons {
    float:left;
    padding-right: 10px;
}


#files_browse {
    float: left;
    padding-right: 10px;
    font-size: small;
    border-right: thin dotted #ccc;
}

.logs_left {
    float: left;
    padding-right: 10px;
}

.logs_middle {
    float: left;
    padding-right: 10px;
    padding-left: 10px;
    border-right: thin dotted #ccc;
    border-left: thin dotted #ccc;
}

.logs_right {
    float: left;
    padding-left: 10px;
}

#log {
    font-size: small;
}

th {
    border-bottom: thin dotted #ccc;
}

table {
    border-bottom: thin dotted #ccc;
}

table > caption {
    text-align: left;
    font-weight: bold;
    margin-bottom: 15px;
}

ul.nobullets{
    list-style: none;
}

#file_list{
    padding-left: 10px;
}

#files_show{
    float: left;
    padding-left: 10px;
}
#files_show p{
    padding-top: 0;
    padding-bottom: 0;
}

h1 {
    padding-bottom: 10px;
}

.state_select {
    width: 135px;
}

.button {
    margin-right: 10px;
    margin-top: 10px;
}

.warning {
    color: #c4110d;
    border: thin dotted #c4110d;
    padding: 10px;
    margin-top: 10px;
    margin-bottom: 20px;
}

#footer p {
    border-top: 1px solid #ccc;
    text-align: center;
    font-size: x-small;
    color: #555;
    padding-top: 5px;
}

.spacer{
    clear: both;
}

.problem {
/*background-color: #fbbfb7;*/
    background-color: #f9aa9f;
}

.noline {
    border: none;
}

.noline td{
    border: none;
    padding: 0;
    vertical-align: top;
}

th.header_top{
    padding-top: 20px;
}

.docproc_nodes{
    border: none;
}

td.extra_padding_top_bold {
    padding-top: 10px;
    font-weight: bold;
}

th.extra_padding_left, td.extra_padding_left{
    padding-left: 10px;
}

.summary td {
    border-bottom: thin dotted #ccc;
}


.radio {
    border: none;
    margin-bottom: 5px;
}

.container {
    border: thin dotted #ccc;
    padding: 10px;
}

.cluster_container {
    border: thin solid #aaa;
   padding: 10px;
}

.cluster_container_for_search_view {
    border: thin solid #aaa;
   padding: 10px;
    margin-left:10px;
    margin-right: 10px;
}

.chain_container_for_docproc_view {
    border: thin solid #aaa;
    padding: 10px;
    margin-left: 10px;
    margin-right: 10px;
    margin-bottom: 10px;
}


.inside_container{
    float: left;    
}

.inside_container img {
    padding-right: 10px;
}

.right_side{
    float: right;
}



.search_nodes{
    border-collapse: collapse;
    border: 1px solid #ccc;
}

.search_nodes th{
    border: 1px solid #ccc;
    background-color: #eee;
    padding: 3px 10px;
}

.first_col {
    font-weight: bold;
    border: 1px solid #ccc;
    background-color: #eee;
    padding: 3px 10px;
}

.search_nodes td{
    border: 1px solid #ccc;
    padding: 3px 10px;
}

.search_nodes tr.summary{
    font-weight: bold;
}

.table_caption {
    text-align: left;
    font-weight: bold;
    margin-bottom: 15px;
    margin-top: 20px;
}

.centered {
    text-align:center;
    padding: 0;
    float: none;
}

.small_top_margin {
    margin-top: 10px;
}

a img {
    border: none;
}

.vertical_spacer {
    margin-top: 5px;
    margin-bottom: 5px;
}


.docproc {
   width: 200px;   
}

.top_align td{
    vertical-align: top;
    margin-top: 3px;
    margin-bottom: 3px;
}

.noline_and_spacing {
    border: none;
    margin-bottom: 10px;   
}


.noline_and_spacing td{
    border: none;
    padding: 0;
    vertical-align: top;
}


.index_left{
    width: 410px;
    float: left;
    height: 240px;
    border: thin dotted #ccc;
    padding: 10px;
}

.index_right{
    width: 410px;
    float: left;
    height: 240px;
    border: thin dotted #ccc;
    padding: 10px;
}

.indexmiddle{
    width: 42px;
    float: left;
    margin-top: 110px;
}

.index_arrow_side{
    width: 432px;
    float: left;
    height: 42px;
}

.index_arrow_middle{
    width: 42px;
    float: left;
    height: 42px;
}

.small_text {
    font-size: x-small;
}

/* VESPA.LOG VIEW */
.log_FATAL, log_ERROR{
    color: #c4110d;
    font-weight: bold;
}

.log_WARNING{
    color: #c4110d;
}

.log_EVENT {
    color: #555;
}

.nowrap {
    white-space: nowrap;    
}

/* CALENDAR IN VESPA.LOG VIEW */
.calendar table table{
    border: none;
}

.rich-calendar-exterior {
    border-right: thin solid #ccc;
    border-left: thin solid #ccc;
    border-top: thin solid #ccc;
    border-bottom: none;
}

.rich-calendar-header {
    background-color: #eee;
    border-bottom: thin solid #ccc;
}

.rich-calendar-footer {
    border-bottom: none;
}

.rich-calendar-days, .rich-calendar-footer{
    background-color: #fbfbfb;
    border-bottom: thin solid #ccc;
    border-top: none;
}

.rich-calendar-weekends {
    color: #1d3688;
    border-bottom: thin solid #ccc;
}

.rich-calendar-month {
    font-weight: bold;
}

.rich-calendar-cell {
    border-bottom: thin solid #ccc;
    border-right: thin solid #ccc;    
}

.rich-calendar-holly{
    background-color: #ecf4fe;
    color: #1d3688;
    border-bottom: thin solid #ccc;
    border-right: none;
}

.rich-calendar-date-layout {
    border: thin solid #ccc;
    background-color: white;
}

.rich-calendar-editor-btn {
    background-color: white;
}

.rich-calendar-date-layout-split {
    background-color: white;
    border-right: thin solid #ccc;    
}

.rich-calendar-hover {
    background-color: #eee;
}

.rich-calendar-today {
    font-weight: bold;
    background-color: #eee;
}

.rich-calendar-select {
    background-color: #1d3688;
    color: white;
    border-bottom: thin solid #ccc;
}


.disabled {
    color: #575757;
    font-weight: normal;
    font-style: italic;
    border-bottom: thin solid #ccc;
}

.rich-calendar-boundary-dates{
    color: #ccc;
}

.rich-calendar-editor-btn-over {
    border: thin solid #ccc;
    background-color: #eee;    
}

.rich-calendar-editor-btn-selected{
    background-color: #1d3688;
    color: white;
    border: thin solid #ccc;
}

.rich-calendar-editor-tool-over {
    border: thin solid #ccc;
    background-color: #eee;
    color: black;    
}

.rich-calendar-date-layout-ok, .rich-calendar-date-layout-cancel {
    background-color: #eee;
}

/* TREE IN ACTIVE CONFIGURATIONS */
.rich-tree table {
    border: none;
}

.rich-tree-node-children { /* this surrounds each "row" */
    margin-left: 20px;
}

.rich-tree-node-handleicon {      /* all table cells with lines and/or expand/collapse button*/
    margin: 0;
    padding: 0;
    background-position:right;
}

img.dr-tree-pointer-cursor { /* expand/collapse */
    margin-left:10px;
}

.rich-tree-node-icon img{ /* folder node */
    margin-left:3px;
}

.rich-tree-node-icon-leaf img{ /* leaf node */
    margin-left:3px;
}

.rich-tree-node-children table.rich-tree-node{ /* one table for each leaf node */
    padding-left: 10px;
}

.rich-tree-node-text{   /* node text properties */
    padding-left: 0;
    margin-left: 0;
}

.rich-tree-node-selected{     /* selected node */
    font-weight: bold;
    border: none;
}

/* HIGHLIGHT SETTINGS FOR XML-FILES */
.xml_plain{color:#000;}
.xml_char_data{color:#000;}
.xml_tag_symbols{color:#003bff;}
.xml_comment{color:#939393;background-color:#f7f7f7;}
.xml_attribute_value{color:#c10000;}
.xml_attribute_name{color:#000;font-weight:bold;}
.xml_processing_instruction{color:#000;font-weight:bold;font-style:italic;}
.xml_tag_name{color:#0037ff;}
.xml_rife_tag{color:#000;background-color:#e4e6a0;}
.xml_rife_name{color:#0000c4;background-color:#e4e6a0;}
.dr-insert-error{color:#c4110d;font-weight:bold;}

